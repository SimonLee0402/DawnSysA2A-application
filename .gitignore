# Dependencies
node_modules/
.pnp
.pnp.js

# Build files
# dist/ # Keep dist for Vue build output, but if Django also has a dist, be specific.
# build/ # Keep build for Vue build output, but if Python also has a build, be specific.

# Vue/Frontend specific build output
a2a_workflow_platform/frontend_src/dist/
a2a_workflow_platform/frontend_src/build/


# Local development files
.env*
*.env
.env.*.local
*.env.local

# Cache files
.npm/
.eslintcache
.vite/
.cache/ # General cache directory

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log
*.log.*

# Editor directories and files
.idea/
# .vscode/ # Decided to manage .vscode content manually or ignore all for now
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
# build/ # Python build folder, if different from frontend
# dist/ # Python distribution folder, if different from frontend
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtualenv
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Django
*.sqlite3
*.sqlite3-journal
# media/ # Consider if all media should be ignored or if some placeholders are needed
# a2a_workflow_platform/media/ # More specific path for media if needed
# static_collected/ # Django's collected static files
# a2a_workflow_platform/static_collected/ # More specific path
# staticfiles/ # Alternative name for collected static files

# Specific Django project static/media folders if they are not app-specific
# and contain generated or user-uploaded content.
# If media/ and static_collected/ are at the root of the Django project (a2a_workflow_platform),
# then these paths are more accurate:
a2a_workflow_platform/media/
a2a_workflow_platform/static_collected/

a2a_workflow_platform/test_media/ # Test media files

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Coverage reports
.coverage
coverage.xml
htmlcov/

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# pytest cache
.pytest_cache

# MyPy cache
.mypy_cache

# Django migrations (if you prefer not to track all of them, e.g. squashed migrations)
# some_app/migrations/0*.py # Example, adjust as needed

# IDE workspace files
*.sublime-workspace
*.sublime-project

# Static files generated by 'collectstatic' in Django, if not already covered
# Adjust 'staticfiles' if your STATIC_ROOT is different
# staticfiles/

# If you have a global 'static' or 'media' folder at project root that's NOT part of an app
# /static/
# /media/

# Sensitive files
credentials.json
secrets.yaml
*.pem
*.key
config.ini # If it contains secrets

# Backup files
*.bak
*.swp
*~ 